# RC Motogarage — Project Skill

Use this skill whenever working on the RC Motogarage website project.

## Project location
- Root: `/home/michael/portfolio/rc-motogarage-website/`
- GitHub: `https://github.com/BabyKatana72/Website-Portfolio` branch `main`
- Local preview: `http://127.0.0.1:8787/index.html`

## Constraint rules
- Do NOT use Figma references anywhere — use Penpot only
- Do NOT rename or delete important existing files without confirmation
- Preserve layout structure when removing media elements: use empty `aria-hidden="true"` decorative placeholders instead of deleting markup
- All code edits go through the dev subagent, never edit source files directly from the main session
- Pause structural website redesign until explicitly told to resume
- No Shopify integration until revenue covers the $39/mo subscription cost
- Indonesian payment gateways (Xendit / Midtrans) only after proven customer demand
- No credit-card-dependent platforms unless explicitly approved

## Brand / design rules
- Background: `#141414`
- Accent: `#ED1C24`
- Body text: white
- Surfaces: `#1E1E1E`, `#2A2A2A`
- Font: Montserrat (Google Fonts)
- Sections: full-screen layout (`min-height: 100vh`), normal scrolling, no CSS scroll-snap unless asked
- Product tiles: image-only by default; hover reveals tags/elements only if needed and only if DESIGN-BRIEF.md explicitly allows text overlays
- Rule: product cards must contain only the product image; no text, price, badge, category, or CTA inside the card itself unless explicitly approved in DESIGN-BRIEF.md
- Do not add “elegant” soft gradients, blur blobs, or film-grain overlays unless explicitly requested

## Workflow rules
- Make small, reversible changes
- Confirm behavior with a local preview or file read after editing
- Report exactly what file paths were changed and what was touched
- Do not over-describe or speculate — just show result
- Do not investigate before acting when given a clear direction
- Number fixes when multiple changes are made

## Image / asset references
- Before creating or editing images, check `/home/michael/portfolio/rc-motogarage-website/assets/references/` for style/design reference images.
- When asked to generate graphics, describe any reference files you found and how you used them.
- If the folder is empty, state that no references were found and proceed with the brief.

## Design brief (MANDATORY)
- Before generating, designing, or editing ANY creative work, read `/home/michael/portfolio/rc-motogarage-website/DESIGN-BRIEF.md` in full.
- Follow the brief's approved palette, typography, texture system, mood spectrum, and component spec.
- Treat the brief as the source of truth for design decisions.

## Communication
- Concise by default
- Match Michael’s energy: faster, fewer words when he’s terse
- Do not include unsolicited suggestions unless asked
- When feedback is negative like “you missed X”, adjust immediately and rerun only what’s needed
